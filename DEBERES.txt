Deberes a corregir en push_swap:
->Es ft_t_stack_error una función necesaria ??
->Borrar print_stack
->Borrar print en check arguments

->Init_stack -> podemos calcular directamente el size ?
Se ha eliminado silent_sa y silent_sb, no parecen necesarias, pues nunca uso ss. queda pendiente del 100% del proyecto

t_node	*find_closest_in_chunk(t_stack *stack, int chunk_start, int chunk_end)
{
	t_node	*current;

	current = stack->top; esto declarado en la misma linea
